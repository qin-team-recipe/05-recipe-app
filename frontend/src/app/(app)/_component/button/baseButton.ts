import { tv } from "tailwind-variants"

/** @package */
export const baseActionButton = tv({
  base: "rounded border w-full px-3 text-sm",
})

/** @package */
export const formActionButton = tv({
  base: "rounded border w-full py-2 text-base",
})
